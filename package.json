{
  "name": "user-management-system",
  "version": "1.0.0",
  "description": "User Management System - Full Stack Application",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "build:backend": "cd backend && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "serverless-http": "^4.0.0"
  }
}
